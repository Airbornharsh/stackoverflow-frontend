import MainBody from '../Body/MainBody';
import SideBar from '../Body/SideBar';

const Body = () => {
  return (
    <div>
      <SideBar />
      <MainBody />
    </div>
  );
};

export default Body;
